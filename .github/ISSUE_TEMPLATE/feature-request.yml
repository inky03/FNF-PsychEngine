name: Feature Request
description: Request... a feature!
labels: [enhancement]
body:
  - type: textarea
    id: request
    attributes:
      label: What feature do you want to get added on the BASE engine?
    validations:
      required: true

  - type: dropdown
    id: requesttype
    attributes:
      label: "What does this feature request encompass?"
      options:
        - "Source Code Modding"
        - "HScript Scripting"
        - "Lua Scripting"
        - "Other"
    validations:
      required: true
  
  - type: input
    id: purpose
    attributes:
      label: How would users of the engine benefit from this feature?
    validations:
      required: true